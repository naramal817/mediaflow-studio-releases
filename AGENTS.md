# mediaflow-studio-releases - 에이전트 공통 규약

> 이 파일은 **코덱스(OpenAI Codex CLI)** 가 읽는 자리다. 클로드 코드는 `CLAUDE.md` 를 읽는다.
> **두 도구가 같은 규약으로 일하도록** 같은 내용을 가리킨다.

**전사 공통 규약 정본: `.claude/shared-rules.md`** - 작업 전에 반드시 읽어라.
그 파일은 `naramal817/code-root` 의 `CLAUDE.md` 에서 자동 생성된다. **직접 고치지 마라**
(다음 동기화 때 덮어써진다). 규칙을 고치려면 정본을 고치고 `_ops/sync-shared-rules.ps1` 을 실행한다.

프로젝트 고유 규칙은 `CLAUDE.md` 에 있다. 그것도 함께 읽어라.

## 특히 자주 어기는 것

- **확인 전에 단언하지 않는다**(1항). "없다"/숫자/비용은 명령을 실행해 확인한 뒤 말한다.
- **서버PC 실행이 필요하면 개발자님께 명령어를 나열하지 않는다**(18항).
  **`Overwatch/directives/<이 저장소명>-<날짜>-<설명>.md`**(자기 저장소 안이 아니다)에
  정해진 포맷으로 요청을 남긴다(18-1항). agent-bus는 폐기됐다 — `bus_send` 쓰지 마라(17항).
  **직접 남의 저장소를 고쳐 푸시하지 마라.**
- **main/master 직접 푸시 금지**(10항).
- **산출물은 최종 형태로 확인한 것만 보고한다**(20항).
- **안 되는 것이 있으면 '되는 방안'까지 낸다**(21항). "불가"로 끝내지 않는다.